#include "predicates.h"
namespace utils
{	
  void  testPermutations(int* arr, int startIndex, const int& arrLength, predicates::BasePredicate* test);
  void swap(int* x, int* y);
  void print(const int* arr, const int& arrLength);
}

